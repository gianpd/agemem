[project]
name = "agemem"
version = "0.1.0"
description = "AgeMem Interactive Chat — REPL entry point for the AgeMem-hybrid system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.25.0",
    "rich>=13.0.0",
    "prompt_toolkit>=3.0.0",
    "sentence-transformers>=3.0.0",
    "sqlite-vec>=0.1.0",
    "numpy>=1.24.0",
    "beautifulsoup4>=4.12.0",
]

[project.optional-dependencies]
ingest = [
    "docling>=2.0.0",
    "gliner>=0.2.0",
    "PyYAML>=6.0.0",
    "PyMuPDF>=1.23.0",
]

[project.scripts]
agemem = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
